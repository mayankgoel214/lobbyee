// Empty stub aliased in vitest.config.ts — lets the Node test harness import
// server-only modules (lib/db, lib/auth) that Next.js would otherwise restrict
// to Server Component bundles.
export {};
