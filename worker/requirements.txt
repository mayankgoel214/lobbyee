# Lobbyee voice worker — production deps (pinned to the versions the local venv
# runs). pipecat extras pull the matching STT/TTS/LLM/VAD/WebRTC service deps.
pipecat-ai[webrtc,silero,deepgram,cartesia,google]==1.3.0

# Used directly by lobbyee_bot.py / serve()
httpx==0.28.1
python-dotenv==1.2.2
loguru==0.7.3
fastapi==0.136.3
uvicorn[standard]==0.49.0
